<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wk.mapper.EventMapper">

    <!--    <update id="updateEvent">-->
    <!--        UPDATE meta_event-->
    <!--        <set>-->
    <!--            <if test="entity.creator!=null">creator = #{entity.creator},</if>-->
    <!--        </set>-->
    <!--        WHERE id = #{entity.id};-->
    <!--    </update>-->

    <!--    <select id="getMetaEventList" resultType="com.wk.model.MetaEventEntity">-->
    <!--        SELECT DISTINCT *-->
    <!--        FROM meta_event-->
    <!--        <where>-->
    <!--            <if test="dto.is_visible!=null">AND is_visible = #{dto.is_visible}</if>-->
    <!--        </where>-->
    <!--        ORDER BY id DESC-->
    <!--    </select>-->

    <select id="getEventOne" resultType="com.wk.model.MetaEventEntity">

    </select>
</mapper>